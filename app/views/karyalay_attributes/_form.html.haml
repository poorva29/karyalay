= form_for @karyalay_attribute do |f|
  - if @karyalay_attribute.errors.any?
    #error_explanation
      %h2= "#{pluralize(@karyalay_attribute.errors.count, "error")} prohibited this karyalay_attribute from being saved:"
      %ul
        - @karyalay_attribute.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
